# 更新记录

## 2026-09-15

### 安全与数据完整性

- 收紧合同回款视图：匿名请求不再可读取付款金额、币种和备注，读取范围与合同关联线索权限一致。
- 禁止匿名执行自动退回公海和企微通知标记 RPC。
- 定时续费提醒接口加入公开路由白名单，继续使用 `x-job-token` 做服务端鉴权。
- 有效线索配额改为按负责人加事务级 advisory lock 校验，避免并发请求突破上限。
- 没有用户 JWT 时不再默认绕过配额校验。
- 已软删除线索变为不可修改。
- 分配和转移线索时校验目标成员为启用状态且属于目标团队。

### 线索与组织架构

- 新建线索会保存 `customer_attribute` 和 `follow_up_stage`。
- 补齐多团队成员的转移、移除、审批和恢复写入 RPC。
- 多团队转移会同步成员关系和名下线索团队归属。
- 生命周期 E2E 测试恢复真实创建结果断言，避免点击按钮后假通过。

### 工程质量

- 修复 Recharts、角色权限和组织设置页面的 TypeScript 错误。
- 根项目 TypeScript 检查排除 Supabase Deno Edge Function，Edge Function 继续由 Deno 工具链单独检查。
- 完善 README，记录运行、迁移、部署和版本更新流程。

## 2026-09-14

- 增加公海退回原因记录和批量退回能力。
- 增加成员多团队关系表和公海线索表格横向溢出修复。
- 统一 Worker 生产版本检查接口。
